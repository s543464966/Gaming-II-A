PRD项目说明书

# 场景：Home
   ## 模块： 商城系统
   1. 商品结算功能
      1.1 实现流程
      - 接收一次商品结算请求, 目标可以是普通商品或礼包商品。
      - 读取本次请求对应的商品配置和购买限制数据。
      - 校验该商品是否达到购买上限。
      - 计算该商品的结算价格, 折扣和最终金额。
      - 校验玩家货币是否足够支付本次结算。
      - 货币足够时扣除对应货币。
      - 按商品类型发放内容, 普通商品直接发放, 礼包商品按礼包内容拆分发放。
      - 刷新商城界面和玩家货币显示。
      - 返回本次结算结果。
      1.2 计划实现的能力
      - 支持单次结算一个商品实体, 商品类型可为普通商品或礼包商品。
      - 支持读取商品配置和购买限制。
      - 支持校验商品购买上限, 兼容每日特惠类限制。
      - 支持结算前计算商品价格与最终金额。
      - 支持校验玩家货币是否足够。
      - 支持结算成功后扣款并发放商品内容。
      - 支持结算失败时不给予商品且不扣款。
      - 支持结算后刷新界面显示。
# 场景：LevelFighting
## 模块：章节流程层优化
    ### 子闭环一：入口与选关闭环
      负责从进入 LevelFighting 场景开始, 串起章节入口恢复、路线展示、节点选择与节点内容页面切换, 形成 `进入场景 -> 路线/节点内容 -> 选定下一节点` 的前置流程闭环。
       1. 场景入口与初始阶段判定 (Checked)
         1.1 目标
         统一 LevelFighting 场景入口逻辑, 让章节入口能够根据当前选择章节的恢复状态, 稳定决定进入路线选择状态或当前节点内容页面, 并明确区分关卡准备页面与备战席页面。
         1.2 关键实现
         - LevelFighting 场景入口改为统一初始化后再做入口判定, 不再在 Awake 中无条件创建当前关卡。
           [CC_Conflict/Init_ConflictEntry]
         - 当前选择章节的入口恢复状态已提升为 SaveData 顶层数据, 与 selectedChapterIndex 和 currentLevelIndex 同层记录, 不再借放在单个章节存档中。
           [SaveData/currentChapterEntryState]
           [Sys_Chapter/currentEntryState]
         - 首次进入新章节时, Home 仍负责生成路线和设置第一关, 但进入路线页还是节点内容页由 LevelFighting 场景自行判定。
           [CC_Home/OnBtn_EnterChapter]
           [CC_Conflict/Enter_NodeContentPage]
         - 章节入口恢复只到节点页面级别: 战斗类节点进入关卡准备页面, 非战斗类节点进入对应节点内容页面, 不恢复战斗执行中的临时数据。
           [CC_Conflict/Check_IsBattleLevel]
           [CC_Conflict/Enter_NodeContentPage]
         - 胜利或失败触发时, 章节入口状态会立即写回 GameData 为路线选择状态, 但不立即落盘, 仍由统一存档流程回写 SaveData。
           [CC_Conflict/Result_Level_Victory]
           [CC_Conflict/Result_Level_Defeat]
           [DBCC_DataBase/Chapter_BuildSaveDataFromGameData]
         1.3 关键能力
         - 支持当前选择章节在下次进入 LevelFighting 时恢复到路线选择状态或节点内容页面。
           [CC_Conflict/Init_ConflictEntry]
         - 支持首次进入章节时直接进入第一关对应节点内容页面, 而不是先显示章节路线。
           [CC_Home/OnBtn_EnterChapter]
         - 支持根据 currentLevelIndex 恢复当前待挑战节点, 并按节点类型切换到对应页面。
           [Sys_Chapter/Init_Sys_Chapter]
           [CC_Conflict/Enter_NodeContentPage]
         - 支持明确区分关卡准备页面与备战席页面, 避免入口状态与备战交互状态混用。
           [CC_Conflict/Enter_NodeContentPage]
         - 支持将章节入口恢复状态通过统一存档流程写回 SaveData, 保持与当前选择章节的一致性。
           [DBCC_DataBase/Chapter_BuildSaveDataFromGameData]
      2. 路线浏览与关卡选择流程 (Checked)
         2.1 目标
         将章节路线视图与章节流程切换解耦, 让路线层只负责展示、关闭和节点点击上报, 当前节点内容页面的进入统一由 CC_Conflict 调度, Sys_Chapter 只负责节点选择与路线状态的数据更新, 避免 LevelRoute_UI 和 Sys_Chapter 直接跨层控制战场和备战层。
         2.2 关键实现
         - 路线层已收缩为展示与关闭职责, 节点点击后的页面切换不再由 LevelRoute_UI 直接推进到战场内容页。
           [LevelRoute_UI/Show_ChapterLevelRoute]
           [LevelRoute_UI/Hide_ChapterLevelRoute]
           [LevelRoute_UI/OnBtn_ViewServant]
         - 路线节点点击已改为先上报给路线层, 再由 CC_Conflict 作为唯一流程入口处理节点选择与页面切换。
           [C_Level/OnBtn_LevelEnter]
           [LevelRoute_UI/On_LevelNodeClicked]
           [CC_Conflict/Handle_LevelNodeSelected]
         - Sys_Chapter 的节点选择逻辑已收缩为纯数据更新, 只记录当前节点、路线选择结果和相关节点状态, 不再直接依赖场景对象或触发页面跳转。
           [Sys_Chapter/Process_LevelSelected]
         - 战斗类节点点击后仍进入关卡准备页面, 不直接进入备战席页面; 备战席继续作为关卡准备页面内部的子交互入口。
           [CC_Conflict/Enter_NodeContentPage]
         - 路线页返回后继续复用现有节点状态控制交互, 当下一关节点已经选定后, 路线页只允许查看当前路线状态, 不再允许重新改选其他关卡节点。
           [C_Level/Update_LevelUI]
           [CC_Conflict/OnBtn_ViewChapterRoute]
         2.3 关键能力
         - 支持路线视图独立展示和关闭, 不再依赖路线层直接控制战场层显示。
           [LevelRoute_UI/Show_ChapterLevelRoute]
           [LevelRoute_UI/Hide_ChapterLevelRoute]
         - 支持节点点击后进入该节点对应的内容页面; 对于战斗类节点, 内容页面为关卡准备页面, 不是备战席页面。
           [CC_Conflict/Handle_LevelNodeSelected]
           [CC_Conflict/Enter_NodeContentPage]
         - 支持路线层、数据层、场景流程层三者职责分离, 避免节点点击后出现多处同时推进页面切换。
           [LevelRoute_UI/On_LevelNodeClicked]
           [Sys_Chapter/Process_LevelSelected]
           [CC_Conflict/Handle_LevelNodeSelected]
         - 支持关卡节点选择先更新章节数据, 再由场景流程层统一决定下一个阶段显示的页面内容, 且 Sys_Chapter 不再直接依赖场景对象。
           [Sys_Chapter/Process_LevelSelected]
         - 支持路线页同时承担初次展示和节点内容页面返回后的查看入口; 当下一关节点已经选定后, 返回路线页不再允许重新交互选择其他关卡节点。
           [C_Level/Update_LevelUI]
           [CC_Conflict/OnBtn_ViewChapterRoute]
    ### 子闭环二：关卡准备闭环
      负责在节点内容页面已经确定后, 串起战场运行时对象准备、关卡准备页面展示、备战席交互与出战/替换操作, 形成 `进入战斗类节点 -> 关卡准备页面 -> 备战席操作 -> 阵容确认` 的中段流程闭环。
      3. 战场层与关卡运行时管理 (Checked)
         3.1 目标
         将当前关卡在战场上的运行时对象管理收拢为独立模块, 统一负责占位符、玩家/怪物战斗实例、关卡切换时的复用与清理, 让章节流程层只负责节点进入和阶段切换, 不再直接承担运行时对象拼装职责。
         3.2 关键实现
         - 章节流程层新增独立运行时对象协调器, 由 `CC_Conflict` 持有并调度, 具体玩家/怪物占位缓存、运行时列表、关卡运行时对象创建、替换重绑和关卡结算清理统一收拢到 `Conflict_RuntimeBoard`, `CC_Conflict` 不再缓存或二次保存双方运行时引用列表。
           [CC_Conflict/Awake]
           [Conflict_RuntimeBoard/Init_BlankCache]
           [Conflict_RuntimeBoard/Create_LevelRuntimeObjects]
         - 玩家与怪物 Blank 占位符的批量显隐执行也统一收口到 `Conflict_RuntimeBoard`, `CC_Conflict` 只保留“当前阶段该不该显示”的判断, 不再自己遍历 Blank 列表直接切 `SetActive`。
           [CC_Conflict/Hide_HeroCard]
           [CC_Conflict/Show_HeroCard]
           [CC_Conflict/OnBtn_PlayTheGame]
           [Conflict_RuntimeBoard/Set_HeroCardBlankVisible]
           [Conflict_RuntimeBoard/Set_MonsterCardBlankVisible]
         - 开战接入改为由 `Conflict_RuntimeBoard` 直接把玩家/怪物运行时列表和站位缓存交给 `CC_Fight`, `CC_Conflict` 只负责按钮隐藏、阶段切换、Top 栏移动和骰子初始化, 不再在中间层重复转发运行时列表。
           [CC_Conflict/OnBtn_PlayTheGame]
           [Conflict_RuntimeBoard/Try_StartRuntimeFight]
           [CC_Fight/Init_Fighting]
         - 怪物运行时对象改为按单关粒度创建和清理, 进入战斗类节点时统一创建当前关卡怪物实例, 关卡结束或怪物死亡后统一移除运行时引用并销毁对象。
           [Conflict_RuntimeBoard/Clear_MonsterRuntimeObjects]
           [Conflict_RuntimeBoard/Remove_DeadRuntimeObjectSafe]
         - 玩家运行时对象改为跨关复用, 首次出战时创建实例, 后续替换或连续爬塔时通过原站位实例做数据重绑和状态重置, 不再每关完整销毁重建。
           [Conflict_RuntimeBoard/Deploy_HeroCardRuntime]
           [Conflict_RuntimeBoard/Replace_HeroCardRuntime]
           [Conflict_RuntimeBoard/Reset_HeroCardRuntimeState_Prepare]
           [Conflict_RuntimeBoard/Set_HeroCardVisible_Runtime]
         - `C_FightCard` 新增运行时重绑前清理流程, 在重新绑定玩家或怪物数据前统一清理旧技能组件、旧 mana UI 和关卡级运行时状态, 避免多次替换后叠加脏组件和重复表现。
           [C_FightCard/Reset_RuntimeStateBeforeBind]
           [C_FightCard/Clear_AbilityComponents]
           [C_FightCard/Clear_ManaPrefabs]
         - 准备态交互改为常驻组件开关模式, 开始战斗时统一禁用玩家实例上的准备态交互, 战斗结算返回后统一恢复, 不再通过反复 Destroy/Add `Card_Interaction` 切换状态。
           [Conflict_RuntimeBoard/Set_HeroCardInteraction]
           [Card_Interaction/Set_PrepareInteractionEnabled]
         - 怪物入场表现由运行时对象协调器统一遍历怪物实例并播放移动动画, 章节流程层只传入阶段切换所需的偏移量和时长。
           [Conflict_RuntimeBoard/Move_MonsterRuntimeEntry]
         3.3 关键能力
         - 支持统一缓存并管理玩家/怪物占位符位置, 让关卡运行时对象和战斗模块始终通过 `Conflict_RuntimeBoard` 读取站位数据。
         - 支持统一执行 Hero / Monster Blank 占位符的显隐切换, 避免章节流程层重复遍历占位符对象。
         - 支持按单关创建并清理怪物运行时实例, 怪物死亡时同步移除运行时引用并安全销毁对象, 避免残留空引用和脏列表。
         - 支持统一创建、复用和重绑玩家出战实例, 并在出战或替换时同步运行时列表、卡牌动态数据和站位记录。
         - 支持由运行时协调器统一向 `CC_Fight` 暴露双方运行时列表, 避免 `CC_Conflict` 因中间转发继续膨胀。
         - 支持连续章节爬塔时复用玩家运行时对象, 仅重置关卡级战斗数据、交互状态和显示状态, 降低频繁 Instantiate/Destroy 带来的 GC 压力。
         - 支持后续扩展新的战场表现逻辑或对象池策略, 且不再直接侵入章节流程层。
      4. 备战席与出战/替换流程管理 (Checked)
         4.1 目标
         统一管理备战席的 UI 展示和出战/替换状态，使出战流程和替换流程有唯一状态来源，避免状态散落在 C_Blank、Card_Attribute、CC_Conflict 中。
         4.2 关键实现
         - 备战席流程改为由 `PrepareFlowMode + targetPosIndex` 作为唯一状态源, `Conflict_Prepare` 统一提供出战入口、替换入口和提交入口, 不再通过 `btnState + bool` 组合推断当前流程。
           [Conflict_Prepare/Open_DeployPrepare]
           [Conflict_Prepare/Open_ReplacePrepare]
           [Conflict_Prepare/Commit_SelectedCard]
         - 空占位符 `C_Blank` 只负责打开出战流程, 已出战卡牌点击属性表后只负责进入 `SelectReplace`, 备战席中的候选卡属性表只负责进入 `ConfirmReplace`, 不再把“选择替换目标”和“确认替换候选卡”混在同一个分支里判断。
           [C_Blank/OnBtn_ShowPrepareArea]
           [Card_Interaction/OnPointerUp]
         - 备战席卡牌列表刷新时统一根据当前流程判定卡牌可操作状态, 已出战卡牌在列表中更新为不可再次出战的表现结果, 但业务状态不再依赖按钮禁用本身。
           [Conflict_Prepare/Update_PrepareCard]
           [Conflict_Prepare/Check_IsSelectableCard]
           [C_Card/Set_PrepareSelectable]
         - `Card_Attribute` 改为动作枚举驱动, 统一通过 `CardAttributeActionMode` 区分购买、强化、出战、`SelectReplace` 和 `ConfirmReplace`, 不再保留旧 `btnState` 兼容包装层, 也不再额外引入属性表上下文对象。
           [Conflict_PrepareState/CardAttributeActionMode]
           [Card_Attribute/Update_CardAttribute]
           [Card_Attribute/OnBtn_BuyIntensifyFightingReplace]
         - Home 与章节准备继续共用同一套属性表界面结构, 备战席卡牌列表、出战状态判定和属性表按钮行为保持解耦, 为后续筛选、排序和编队辅助预留稳定扩展位。
           [CC_Card/Adjust_CardPool]
           [C_Card/Init_CollectionCard]
           [C_Card/OnBtn_CardAttribute]
         4.3 关键能力
         - 支持从 `C_Blank` 所控制的空占位符进入出战流程, 选择可出战卡牌后完成目标站位上阵。
         - 支持从已出战卡牌进入替换流程, 锁定目标站位后选择新的备选卡牌完成替换重绑。
         - 支持用 `PrepareFlowMode` 统一驱动备战席模式和目标站位, 用 `CardAttributeActionMode` 统一驱动属性表主按钮行为, 并明确区分 `SelectReplace` 与 `ConfirmReplace`, 避免状态散落。
         - 支持已出战卡牌在备战席中稳定显示为不可再次出战状态, 且 UI 表现不再作为唯一业务判断依据。
         - 支持属性表在 Home 与章节准备之间复用同一套界面, 并通过动作枚举区分仓库预览/购买强化与战斗准备/出战替换两类行为。
         4.4 后续扩展预留
         - 预留卡牌筛选扩展入口。
         - 预留卡牌排序扩展入口。
         - 预留编队辅助扩展入口。
         - 当前框架需避免将卡牌数据源、显示列表和交互状态强耦合为单一路径, 以便后续在不重写主流程的前提下接入上述功能。
    ### 子闭环三：战斗接入与结果回流闭环
      负责在关卡准备完成后, 串起单场战斗接入、结果回收、奖励结算、返回路线或退出场景, 并为非战斗节点类型预留统一接入方式, 形成 `进入战斗 -> 结果结算 -> 回路线/回 Home -> 持续扩展节点类型` 的后段流程闭环。
      5. 战斗接入与结果回收流程 (Checked)
         5.1 目标
         明确章节流程层与单场战斗层的交接边界, 让玩家通过章节路线进入战斗类关卡节点后, 能够从关卡准备和备战确认稳定进入单场战斗, 且 `CC_Fight` 只负责一场战斗执行, 章节流程层只负责开始战斗和接收结果。
         5.2 关键实现
         - 节点内容页改为先按 `LevelType` 分流, 战斗类节点进入关卡准备与战斗接入, 非战斗节点进入扩展占位流程。
           [CC_Conflict/Enter_NodeContentPage]
           [CC_Conflict/Enter_BattleNode]
           [CC_Conflict/Enter_ExtensionNodePlaceholder]
         - 开战按钮增加当前关卡为空、非战斗节点和运行时接入失败的安全返回, 只有成功接入后才隐藏准备 UI、禁用准备态交互、移动 Top 栏并初始化骰子。
           [CC_Conflict/OnBtn_PlayTheGame]
           [Conflict_RuntimeBoard/Try_StartRuntimeFight]
         - 单场战斗仍由 `CC_Fight` 判定胜负并回调章节流程层, 章节流程层通过结算标记防止同一场战斗重复进入胜利或失败结算。
           [CC_Fight/SafeDestroy]
           [CC_Conflict/Result_Level_Victory]
           [CC_Conflict/Result_Level_Defeat]
           [CC_Conflict/Try_EnterResultSettlement]
         5.3 关键能力
         - 支持章节流程层与战斗执行层解耦, 由章节流程层判断当前节点是否为战斗类节点, 战斗执行层只处理本场战斗。
         - 支持玩家通过章节路线选中战斗类关卡节点后, 先进入关卡准备和备战流程, 再通过 `CC_Conflict` 的开战按钮统一进入单场战斗。
         - 支持统一接收单场战斗胜利或失败结果, 并由章节流程层继续调度奖励结算、失败结算或路线返回。
         - 支持防止死亡对象连续销毁时重复触发同一场战斗结算。
         - 支持后续重构战斗执行层时不影响章节路线、关卡准备和结果回流流程。
      6. 奖励结算与章节闭环返回 (Checked)
         6.1 目标
         建立关卡胜利后的奖励结算和章节闭环返回逻辑，使玩家在完成单关后可以选择回到路线继续爬塔，也可以选择退出场景回到 Home。
         6.2 关键实现
         - 胜利结果回流后, 章节流程层统一刷新存活英雄卡牌数据、通知章节系统写入通关数据并进入奖励选择占位流程。
           [CC_Conflict/Result_Level_Victory]
           [Conflict_RuntimeBoard/Update_AliveHeroCardData]
           [Sys_Chapter/Process_CompletedLevel]
           [CC_Conflict/StartHextechSelection]
         - 三选一奖励流程改为运行时绑定按钮, 数据或 UI 配置不足时直接跳过奖励占位并进入胜利结算 UI, 避免胜利流程卡死。
           [CC_Conflict/Bind_RewardSelectionButtons]
           [CC_Conflict/StartHextechSelection]
           [CC_Conflict/Handle_RewardSelected]
           [CC_Conflict/Set_ResultUIVisible]
         - 胜利 UI 的继续按钮统一重置战斗运行时、怪物对象、英雄准备态和 Top 栏后返回章节路线, 不受返回 Home 条件阻断。
           [CC_Conflict/OnBtn_VictoryConfirm]
           [CC_Conflict/Reset_AfterFightToRoute]
         - 失败结果回流后, 章节流程层统一调度卡牌系统处理失败后的出战卡牌数据, 调度章节系统保留当前未完成节点作为下次进入的节点内容入口, 并显示失败 UI。
           [CC_Conflict/Result_Level_Defeat]
           [Sys_Card/Process_LevelDefeat]
           [Sys_Chapter/Process_CompletedLevel]
           [CC_Conflict/OnBtn_DefeatConfirm]
           [CC_Conflict/Return_Home]
         - 通用按钮显示逻辑中, 第一关或 Rest 条件只控制返回 Home 按钮, 不控制路线查看按钮。
           [CC_Conflict/Com_ShowBtn]
           [CC_Conflict/Check_CanReturnHome]
         6.3 关键能力
         - 支持战斗胜利后三选一奖励占位, 并在奖励数据不足或 UI 配置不足时稳定进入胜利结算。
         - 支持胜利结算 UI 提供继续入口, 玩家点击继续后回到章节关卡路线继续爬塔。
         - 支持失败结算 UI 提供返回入口, 玩家点击后退出 LevelFighting 并回到 Home 场景。
         - 支持返回 Home 按钮只在第一关或 Rest 节点显示, 路线查看按钮不受该条件影响。
         - 支持章节内主闭环, 让单关战斗完成后能够回到章节路线继续推进。
         - 支持场景级外层闭环 `Home -> LevelFighting -> Home`。
         6.4 后续扩展预留
         - 预留遗迹奖励结算入口。
         - 预留事件奖励结算入口。
         - 预留其他非战斗节点复用奖励或结算出口的接入位置。
         - 当前设计只保留流程位置和扩展边界, 不要求本轮实现遗迹奖励、事件奖励等具体内容。
      7. 节点类型扩展接入框架 (Checked)
         7.1 目标
         为未来的 Rest、BlackMarket、Relic、Event 等 LevelType 节点预留统一接入方式，避免后续每增加一种节点类型都继续堆积在 CC_Conflict 中。
         7.2 关键实现
         - 当前战斗类节点判断集中在独立方法中, `Start`、普通怪、精英怪和 Boss 均进入战斗准备流程。
           [CC_Conflict/Check_IsBattleLevel]
         - 非战斗节点统一进入扩展占位流程, 当前只刷新基础 UI、隐藏备战显示并保留未来节点处理器接入注释。
           [CC_Conflict/Enter_ExtensionNodePlaceholder]
         7.3 关键能力
         - 支持战斗类节点进入关卡准备、备战与战斗流程。
         - 支持为 Rest、BlackMarket、Relic、Event/Advanture 等非战斗节点保留统一扩展位置。
         - 支持通过注释和独立分支标记未来可能新增的节点类型, 本轮不实现非战斗节点的具体业务逻辑。
         - 支持章节流程层长期泛化与扩展, 避免后续节点类型继续直接堆积在 `CC_Conflict` 中。
## 模块：备战席
### 1.备战席显示内容优化 (Checked)
      1.1 目标
      将备战席从编辑器预挂固定卡牌位, 调整为运行时根据玩家已解锁卡牌数据动态显示备战卡 UI。备战席只负责显示真实存在的玩家卡牌, 并在打开时刷新当前可选状态; 排列和 Content 尺寸交给 Unity 布局组件链路处理。

      1.2 关键实现
      - LevelFighting 初始化阶段继续由 `CC_Conflict` 获取玩家已解锁卡牌列表, 并传入 `Conflict_Prepare.Init_Prepare`。
        - [CC_Conflict/Start]
        - [Conflict_Prepare/Init_Prepare]
      - `Conflict_Prepare` 停止手动计算 `PrepareContentObj` 高度, 仅缓存备战席初始位置、布局组件和玩家已解锁卡牌数据。
        - [Conflict_Prepare/Init_Prepare]
      - `prepareCardList` 保留为场景安全的公开字段, 运行时作为备战卡 UI 实例缓存列表使用。已有预挂对象会被复用, 数量不足时补充实例, 数量多余时隐藏实例。
        - [Conflict_Prepare/Ensure_PrepareCardInstances]
        - [Conflict_Prepare/Create_PrepareCardInstance]
        - [Conflict_Prepare/Hide_UnusedPrepareCardInstances]
      - `Update_PrepareCard` 统一负责备战席内容刷新: 补齐实例、绑定 `Card` 数据、绑定 `Card_Attribute` 引用、设置当前属性面板动作模式, 并刷新卡牌是否可选。
        - [Conflict_Prepare/Update_PrepareCard]
      - `Show_PrepareArea` 打开备战席前刷新卡牌 UI, 让出战模式和替换模式共用同一套动态显示流程。
        - [Conflict_Prepare/Show_PrepareArea]

      1.3 关键能力
      - 备战席显示数量由玩家已解锁卡牌数据决定, 不再被编辑器预挂卡牌位数量截断。
      - 备战卡 UI 支持实例缓存复用, 刷新时只补充缺少实例, 不销毁多余实例。
      - 玩家卡牌数量减少时, 多余备战卡 UI 会被隐藏, 后续数量增加时可继续复用。
      - 备战席只显示真实卡牌 UI, 不为了补齐行列生成空格子。
      - 已出战卡牌在备战席中保留展示, 但会刷新为不可再次选择状态。
      - 本轮不实现筛选、排序、分类页签和稀有度展示, 后续扩展不影响当前动态显示闭环。
## 模块：战斗执行层优化
    ### 子闭环一：单场战斗生命周期闭环
      负责从章节流程层移交单场战斗开始, 串起战斗初始化、回合开始、阵营切换、战斗结束与结果回调, 形成 `接入战斗 -> 回合循环 -> 胜负结束 -> 回流章节` 的执行闭环。
      1. 单场战斗上下文与状态机
         1.1 目标
         将单场战斗运行状态从 `CC_Fight` 的零散字段中收拢为明确的战斗上下文和轻量状态机, 统一记录参战双方、当前阵营、当前回合、当前骰子、行动队列、玩家等待状态和结算标记, 让战斗执行层拥有唯一的运行时状态来源。
         1.2 实现流程
         - 接收 `Conflict_RuntimeBoard` 已准备好的玩家和怪物运行时对象。
         - 初始化单场战斗上下文, 集中记录双方对象列表、双方骰子列表、站位数据、当前执行阵营、当前回合数、当前使用骰子、行动队列和章节流程回调入口。
         - 建立轻量战斗状态枚举, 区分未开始、初始化中、回合开始、等待玩家选择、自动执行中、行动结算中、结果结算中和已结束。
         - 通过状态阶段约束玩家点击、自动行动、骰子执行和结果结算, 避免多个 bool 组合推断当前流程。
         - 开战时重置上一场战斗遗留的当前骰子、行动队列、意图目标、行动计数、玩家等待状态和结算标记。
         - 战斗结束时通过统一入口判断胜利或失败, 并只向章节流程层回调一次结果。
         1.3 计划实现的能力
         - 支持单场战斗上下文集中管理, 避免 `CC_Fight` 继续依赖多组零散字段推断状态。
         - 支持用轻量枚举状态机约束单场战斗的粗粒度阶段, 包括回合推进、玩家等待、自动执行、行动结算和结果回流。
         - 支持将先手、额外回合、跳过回合等机制作为回合交替规则预留, 不直接混入战斗状态枚举。
         - 支持当前阵营和战斗阶段分离记录, 让玩家手动等待、玩家自动执行和怪物自动执行都能进入明确状态。
         - 支持下一关战斗重新初始化运行时状态, 不继承上一场临时数据。
         - 支持胜利或失败只触发一次结果回调。
         - 支持章节流程层继续只负责开战接入和结果回流, 不反向承担战斗执行细节。

      2. 回合推进与阵营切换流程
         2.1 目标
         将当前回合推进逻辑从 `CC_Fight` 中拆为独立回合控制流程, 统一判断玩家回合、怪物回合、自动执行、手动等待、继续使用骰子、切换阵营和进入下一轮。
         2.2 实现流程
         - 每轮开始时请求骰子模块刷新双方骰子面值。
         - 清理双方卡牌和骰子在上一轮留下的意图目标、使用标记和临时状态。
         - 玩家骰子完成发牌表现后, 展示怪物本轮意图。
         - 根据自动战斗开关和当前阵营切换战斗状态, 进入玩家手动等待、玩家自动执行或怪物自动执行。
         - 每次行动完成后检查当前阵营骰子是否全部使用。
         - 当前阵营骰子用尽时切换到另一阵营, 怪物阵营用尽后进入下一轮。
         - 任一方对象全部阵亡时停止回合推进并进入战斗结果流程。
         2.3 计划实现的能力
         - 支持玩家回合、怪物回合和新回合之间稳定切换。
         - 支持玩家手动模式停在可操作阶段, 自动模式连续推进。
         - 支持行动完成后统一回到回合推进入口, 避免多个回调分支各自推进。
         - 支持骰子用尽、目标阵亡、阵营切换和回合交替都有明确判断顺序。
         - 支持在回合开始、行动完成后、阵营切换前后预留回合交替规则入口, 初期可先以注释或空方法标记先手、额外回合、跳过回合等后续扩展点。

    ### 子闭环二：骰子投放与意图选择闭环
      负责从骰子生成、随机面值、意图目标选择到玩家手动或自动确认, 形成 `生成骰子 -> 随机面值 -> 选择目标 -> 提交行动` 的闭环。
      3. 骰子运行时生成与清理
         3.1 目标
         将骰子实例化、占位、随机面值、重置和销毁整理为独立骰子运行时管理能力, 让骰子模块只负责骰子对象和骰子表现的生命周期, 不再承担战斗主流程推进。
         3.2 实现流程
         - 开战后读取玩家出战骰子数据和当前关卡怪物骰子配置。
         - 根据双方骰子数量生成玩家骰子对象和怪物骰子对象。
         - 为每个骰子绑定骰子数据、阵营、初始位置和战斗执行层引用。
         - 每轮开始时为双方骰子随机面值并刷新骰面显示。
         - 玩家骰子移动到可操作占位, 怪物骰子用于意图展示和自动执行。
         - 单颗骰子使用后重置位置、缩放、透明度和使用状态。
         - 战斗结束或返回路线时销毁骰子对象并清空运行时列表。
         3.3 计划实现的能力
         - 支持每场战斗按双方配置生成正确数量的骰子。
         - 支持每轮开始刷新骰子面值和显示。
         - 支持玩家骰子发牌表现与怪物意图表现复用同一批骰子运行时数据。
         - 支持骰子使用后安全复位, 避免死亡目标残留意图骰子。
         - 支持战斗结束后统一清理骰子对象和临时引用。

      4. 意图目标选择与目标规则解析
         4.1 目标
         将玩家手动选择、玩家自动选择和怪物意图选择收口到统一目标规则解析流程, 支持单体、群体、队列、己方、敌方、前排、后排、最近、最远等目标规则, 为后续新骰子面和新技能提供稳定扩展点。
         4.2 实现流程
         - 将骰子面值或行动配置解析为目标类型、阵营方向、作用范围和目标数量。
         - 根据当前执行者阵营区分己方目标和敌方目标。
         - 从战斗上下文中读取当前存活的玩家和怪物对象列表。
         - 按前排、后排、列、最近、最远、是否包含自身等规则筛选目标。
         - 玩家手动选择时校验目标是否合法, 合法后提交目标。
         - 玩家自动和怪物自动时根据规则自动产出目标列表。
         - 目标为空、目标死亡或配置不合法时返回安全结果, 由回合推进层决定跳过或重新选择。
         4.3 计划实现的能力
         - 支持玩家手动目标、玩家自动目标和怪物意图目标共用同一套规则。
         - 支持单体、群体、队列类骰子逐步接入, 不继续在 `C_FightDice` 内堆叠区间判断。
         - 支持技能目标选择和骰子目标选择复用目标查询能力。
         - 支持目标死亡、目标列表为空和目标配置错误时不中断战斗主流程。
         - 支持怪物意图展示只消费目标结果, 不改变目标选择规则本身。

    ### 子闭环三：行动队列与效果执行闭环
      负责把骰子意图转换成可执行行动, 并等待动画、技能、伤害和死亡处理完成, 形成 `提交行动 -> 入队/执行 -> 等待完成 -> 推进下一行动` 的闭环。
      5. 行动命令与执行队列
         5.1 目标
         将当前单体、群体和队列行动分支统一抽象为战斗行动命令, 由行动队列按顺序或并行规则执行, 让行动完成回调成为回合推进的唯一出口。
         5.2 实现流程
         - 接收骰子意图选择流程提交的执行者、骰子面值和目标列表。
         - 根据骰子面值映射行动类型, 生成单体、群体或队列行动命令。
         - 单体行动直接提交一个执行者和一个目标。
         - 群体行动按策略同时或批量提交多个目标或多个执行者。
         - 队列行动按顺序把多个执行者或多个目标加入行动队列。
         - 行动开始前订阅执行完成回调, 行动结束后取消订阅并减少待完成计数。
         - 当前行动全部完成后统一通知回合推进流程继续。
         5.3 计划实现的能力
         - 支持单体、群体和队列行动通过统一入口提交。
         - 支持行动命令携带执行者、骰子面值、目标列表、行动类型和完成回调。
         - 支持行动目标死亡或执行者死亡时安全结束当前行动。
         - 支持一次行动只触发一次完成通知, 防止回合重复推进。
         - 支持后续扩展连击、追加行动、延迟行动和并行动作。

      6. 卡牌行动执行适配
         6.1 目标
         将 `C_FightCard` 从根据骰子面值硬编码执行动作, 调整为接收行动命令后执行具体动作, 让卡牌只负责攻击、治疗、回蓝、技能释放和表现动画, 不再承担战斗主流程判断。
         6.2 实现流程
         - 卡牌行动入口接收行动命令中的行动类型、骰子面值和目标数据。
         - 根据行动类型分流到普通攻击、治疗、回蓝、技能释放或后续新增动作。
         - 普通攻击执行移动、命中、受击反馈和返回原位表现。
         - 治疗、回蓝和属性变化直接调用数值结算管线并刷新表现。
         - 技能释放调用技能系统, 等待技能特效和命中结算完成。
         - 任一动作完成、目标失效或执行者死亡后, 都通过统一完成回调返回行动队列。
         6.3 计划实现的能力
         - 支持卡牌动作执行与战斗主流程解耦。
         - 支持新增骰子行动时通过行动映射接入, 不直接修改回合推进流程。
         - 支持普通攻击、治疗、回蓝和技能释放都稳定返回行动完成信号。
         - 支持攻击动画、受击反馈、可选状态和不可选状态继续保留在卡牌表现层。
         - 支持行动过程中目标死亡或执行者死亡时安全收束。

    ### 子闭环四：战斗数值、技能与词条结算闭环
      负责统一处理战斗时机触发、效果注册、伤害、治疗、护盾、法力、技能命中、词条触发和死亡, 形成 `战斗时机 -> 效果触发 -> 数值结算 -> 技能/词条回流 -> 死亡/存活结果` 的闭环。
      7. 战斗时机与效果注册框架
         7.1 目标
         在战斗主状态和回合流转之外建立独立的战斗时机与效果注册框架, 让技能、词条、Buff、装备、天赋、入场、退场、先手、回合结算等机制都能按固定时机注册和触发, 避免把玩法触发点混入 `BattleState` 主流程枚举。
         7.2 实现流程
         - 将 `BattleState` 继续作为单场战斗粗粒度流程状态, 只负责战斗入口、回合推进、玩家等待、自动执行、行动结算、结果结算和战斗出口。
         - 新增战斗时机概念, 用于标记战斗入场、回合开始、出骰前、出骰后、阵营回合开始、行动前、行动后、阵营回合结束、回合结束、伤害前后、死亡前后和战斗退出等机制触发点。
         - 建立战斗效果抽象, 让技能附加效果、词条效果、Buff 效果、装备效果和天赋效果都能以统一结构记录所属时机、拥有者、优先级、触发条件和执行逻辑。
         - 建立战斗效果注册表, 按战斗时机分类保存当前战斗内已注册的效果。
         - 战斗流程走到对应时机时, 由战斗执行层触发注册表, 按优先级遍历该时机下的效果。
         - 效果执行前先校验拥有者、目标、触发条件和当前战斗状态, 不合法时跳过或移除。
         - 效果执行可以预留协程等待能力, 让动画、技能特效、多段结算等异步流程完成后再继续后续效果。
         - 卡牌死亡、替换、重绑或战斗结束时, 从注册表移除对应拥有者的临时效果。
         7.3 计划实现的能力
         - 支持将战斗主状态和玩法触发时机分层管理, 避免状态机因技能、词条、Buff 扩展而膨胀。
         - 支持同一战斗时机下多个效果按优先级和触发条件稳定执行。
         - 支持技能、词条、Buff、装备、天赋等不同来源复用同一套效果注册和触发机制。
         - 支持入场、退场、先手、出骰前后、行动前后、伤害前后、死亡前后和回合结算等未来机制拥有统一接入点。
         - 支持效果执行期间等待动画、特效和多段结算完成, 避免异步表现打乱战斗流程。
         - 支持死亡、替换、重绑和关卡切换后清理注册表, 避免旧对象残留触发。

      8. 数值修改与死亡结算管线
         8.1 目标
         将生命、护盾、法力、攻击力等战斗数值修改整理为统一结算管线, 让伤害、治疗、护盾、回蓝、属性增减、死亡标记和 UI 刷新有稳定顺序。
         8.2 实现流程
         - 接收卡牌行动、技能命中或词条效果产生的数值变化请求。
         - 根据变化类型区分伤害、治疗、护盾、法力、攻击力和后续扩展属性。
         - 伤害结算先处理护盾抵消, 再处理生命扣减。
         - 治疗、护盾、法力和属性变化按各自上下限修正最终值。
         - 数值变化完成后刷新血条、护盾条、法力条、攻击力文本和漂浮字表现。
         - 生命归零时标记死亡状态, 清理交互和动画状态。
         - 死亡结果交给战斗执行层统一移除运行时对象并判断胜负。
         8.3 计划实现的能力
         - 支持伤害、治疗、护盾、回蓝和攻击力变化统一结算。
         - 支持死亡对象只进入一次死亡结算, 不重复触发胜负判断。
         - 支持漂浮字和 UI 刷新作为表现结果, 不参与业务判断。
         - 支持复活、遗言、击杀、持续伤害等后续机制拥有明确插入点。
         - 支持失败后的卡牌持久化数据仍由章节结果流程和卡牌系统统一处理。

      9. 技能释放与特效命中流程
         9.1 目标
         将技能释放、目标获取、特效创建、特效播放、命中结算和技能完成通知整理为标准流程, 让多段技能、并发目标、飞行特效和瞬发特效都能稳定完成并回到行动队列。
         9.2 实现流程
         - 卡牌行动请求技能释放时, 技能基类读取技能配置、动画控制器、隐秘强化和天赋强化数据。
         - 具体技能生成目标规则、释放段数、数值类型和数值结果。
         - 目标解析流程返回本次技能命中的目标列表。
         - 特效系统按目标和段数创建特效对象, 并绑定技能、目标、数值类型和数值。
         - 特效按飞行或瞬发模式播放, 命中时调用技能命中接口。
         - 技能命中接口把最终数值变化交给数值结算管线。
         - 全部特效播放和命中结算完成后, 技能通知卡牌行动完成。
         9.3 计划实现的能力
         - 支持技能定义目标、数值、段数和释放顺序, 但不直接推进回合。
         - 支持飞行特效、瞬发特效、多目标和多段释放。
         - 支持技能等待全部特效完成后再返回行动队列。
         - 支持特效配置缺失、目标死亡或目标为空时不中断战斗流程。
         - 支持新增技能时主要扩展具体 `C_Ability_T` 子类, 不修改战斗主流程。

      10. 词条触发与优先级框架
         10.1 目标
         重建战斗词条的注册、排序、触发和移除框架, 让攻击前、受击后、攻击后、击杀、死亡、遗言、护盾、治疗等触发点能够作为战斗效果来源稳定接入具体效果。
         10.2 实现流程
         - 战斗初始化或卡牌重绑时读取卡牌、装备、隐秘、天赋等来源提供的词条配置。
         - 将词条转换为可注册的战斗效果, 并按词条触发类型挂入对应战斗时机。
         - 同一战斗时机下按词条类型和优先级排序。
         - 战斗数值、攻击、治疗、护盾、死亡、击杀等流程在对应时机触发词条效果。
         - 词条效果执行时复用行动、目标、数值和技能结算能力, 不绕过当前结算流程。
         - 卡牌死亡、替换、重绑或关卡清理时移除对应词条引用。
         10.3 计划实现的能力
         - 支持词条注册后能被对应战斗时机实际触发。
         - 支持同一战斗时机存在多个词条时按优先级稳定执行。
         - 支持词条效果接入当前行动或数值结算流程, 不破坏回合推进。
         - 支持死亡、替换和关卡切换后不残留旧卡牌词条引用。
         - 支持后续逐步补充具体词条效果, 不要求一次性实现全部词条内容。
