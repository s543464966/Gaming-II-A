# AGENTS.md

## Scope

- 将当前目录视为 Unity 客户端工程。
- 所有说明性回复统一使用中文。
- 本文件只定义全局入口, 工作流路由和最高优先级原则。
- `PRD.md` 作为项目功能说明, 功能状态和功能开发入口的主文档。
- Unity C# 代码风格, 命名, 注释, 序列化安全规则统一维护在 `.agents/workflows/unity_csharp_style.md`。

## Priority

当规则冲突时, 按以下优先级执行:
1. 上层系统规则和开发者规则
2. 用户当前明确需求
3. 本文件全局规则
4. `.agents/workflows/` 下的具体工作流
5. 本地代码和文档上下文

若无法同时满足, 优先保护 Unity 序列化安全, Inspector 引用安全, Prefab 引用安全和场景引用安全。

---

## Global Rules

- 未得到明确执行确认前, 不得修改项目文件。
- 除非用户明确要求, 不得修改业务逻辑。
- 除非用户明确要求, 不得新增, 删除, 修改成员变量或方法签名。
- 禁止使用 `.py` 或其他外部批处理脚本批量改写源码。
- 必须使用原生文件编辑方式逐文件安全修改。
- 优先采用最小安全改动。

---

## Workflow Routing

### Module Breakdown

如果用户请求属于新增系统, 新增模块, 优化系统, 改善模块, 重构模块, 或要求先拆分模块再开发:
- 必须读取 `.agents/workflows/module_breakdown.md`。
- 先输出可写入 `PRD.md` 的拆分方案。
- 不得自动回写 `PRD.md`。
- 只有用户写入 `PRD.md` 并指定功能节点后, 才进入功能开发工作流。
- 如果用户已经明确指定 `PRD.md` 中的现有场景, 模块, 功能节点, 并要求“整合”, “润色”, “优化后回写”, “把新增想法吸收后覆盖回写”, 不属于模块拆分。
- 即使用户讨论的是“优化”或“重构”, 只要目标是覆盖更新已有 PRD 节点正式内容, 仍按 Feature Development 中的 `PRD 迭代` 处理, 不得误走模块拆分工作流。

### Feature Development

如果用户请求属于新功能, 功能扩展, 系统流程调整, UI 交互或 Unity C# 业务逻辑开发:
- 必须读取 `.agents/workflows/feature_development.md`。
- 默认按 `PRD.md` 驱动执行。
- 必须先读取用户指定的 PRD 模块和功能节点, 再进入功能开发工作流。
- 如果该模块已按 `子闭环` 组织, 且用户指定的是某个子闭环或该子闭环下的功能节点, 必须先读取该子闭环下的相关功能节点完整内容。
- 按功能开发工作流执行。
- 如果用户要求读取现有 `PRD.md` 节点并整合本轮新增想法后直接回写, 视为 `PRD 迭代`, 必须走本工作流, 不得先输出模块拆分方案。
- 对已有 PRD 节点做迭代回写时, 必须保持该工作流的规范。
- 除非用户明确要求重构 PRD 结构或改写标题模板, 不得擅自将现有节点改写为另一套结构, 例如把 `实现流程` / `计划实现的能力` 改成 `功能边界` / `验收方向`。
- 当前节点已有的标题名称, 例如 `目标`, `实现流程`, `计划实现的能力`, `关键实现`, `关键能力`, 应视为该节点当前唯一有效格式; 回写时只能在原结构内整合, 去重和覆盖。

### Unity C# Style

如果任务涉及 Unity C# 脚本的生成, 编辑, 重构, 注释整理或命名规范化:
- 必须读取 `.agents/workflows/unity_csharp_style.md`。
- 必须遵守其中的代码风格, 注释规范和序列化安全规则。

### Other Task Types

- Bug 修复: 使用 `systematic-debugging`。
- 代码审查: 使用 `code-reviewer`。
- 代码验证: 使用 `lint-and-validate`。
- 数据表或 CSV 修改: 使用对应数据表工作流。

---

## Output Behavior

- 修改完成后必须说明修改文件, 实现内容, 验证结果和残余风险。
- 若因序列化安全保留了不合规范的字段名, 必须说明字段名, 保留原因和需要用户确认的后续操作。
